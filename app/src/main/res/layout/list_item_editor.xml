<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/editor_card_height">

    <ImageView
        android:id="@+id/iv_editor_card"
        android:layout_width="@dimen/editor_card_icon_size"
        android:layout_height="@dimen/editor_card_icon_size"
        android:layout_marginBottom="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:tint="@color/red_colorPrimaryDark"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_round_vibration" />

    <TextView
        android:id="@+id/tv_editor_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:textAppearance="@android:style/TextAppearance.Material.Subhead"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/iv_editor_card"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Quick Taps" />

    <ImageView
        android:id="@+id/iv_main_editor_reorder"
        android:layout_width="@dimen/main_card_reorder_size"
        android:layout_height="@dimen/main_card_reorder_size"
        android:layout_marginBottom="12dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="12dp"
        android:src="@drawable/ic_round_reorder"
        android:tint="@color/editor_card_reorder_color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</android.support.constraint.ConstraintLayout>